{
  "name": "iam-access-key-rotation",
  "version": "0.1.0",
  "bin": {
    "iam-access-key-rotation": "bin/iam-access-key-rotation.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest --silent",
    "cdk": "npm run build && npm run test && npx cdk synth && npx cdk deploy",
    "destory": "npx cdk destroy"
  },
  "devDependencies": {
    "@types/jest": "^30",
    "@types/node": "^24.10.1",
    "aws-cdk": "2.1104.0",
    "esbuild": "^0.27.3",
    "jest": "^30",
    "ts-jest": "^29",
    "ts-node": "^10.9.2",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.236.0",
    "constructs": "^10.0.0"
  }
}